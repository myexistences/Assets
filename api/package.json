{
  "name": "github-discord-webhook",
  "version": "1.0.0",
  "description": "GitHub webhook to send specific file commits to Discord",
  "main": "github-webhook.js",
  "scripts": {
    "dev": "vercel dev",
    "start": "vercel dev",
    "vercel-build": "echo 'No build needed for this API'",
    "build": "echo 'No build needed for this API'"
  },
  "dependencies": {
    "axios": "^1.7.0"
  }
}
